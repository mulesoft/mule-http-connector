<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xsi:schemaLocation="
               http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
               http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd">

    <http:listener-config name="listenerConfigRejectingInvalidTE" rejectInvalidTransferEncoding="true">
        <http:listener-connection host="localhost" port="${rejectingPort}" />
    </http:listener-config>

    <http:listener-config name="listenerConfigAllowingInvalidTE">
        <http:listener-connection host="localhost" port="${allowingPort}" />
    </http:listener-config>

    <flow name="flowRejectingInvalidTE">
        <http:listener path="/test" config-ref="listenerConfigRejectingInvalidTE" />
        <logger level="ERROR" message="#[payload]" />
    </flow>

    <flow name="flowAllowingInvalidTE">
        <http:listener path="/test" config-ref="listenerConfigAllowingInvalidTE" />
        <logger level="ERROR" message="#[payload]" />
    </flow>
</mule>
